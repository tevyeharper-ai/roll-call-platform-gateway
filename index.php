<?php
session_name('dkforge');
$secureRequest=(!empty($_SERVER['HTTPS'])&&$_SERVER['HTTPS']!=='off') || (($_SERVER['HTTP_X_FORWARDED_PROTO']??'')==='https');
session_set_cookie_params(['httponly'=>true,'samesite'=>'Strict','secure'=>$secureRequest]);
session_start();
require dirname(__DIR__).'/src/helpers.php';

if(isset($_GET['health']) || parse_url($_SERVER['REQUEST_URI'] ?? '/', PHP_URL_PATH)==='/health'){
  header('Content-Type: application/json');
  echo json_encode(['ok'=>true,'product'=>forge_config()['name'],'version'=>forge_config()['version'],'storage'=>storage_status(),'https'=>https_status()],JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES); exit;
}
if(isset($_GET['logout'])){audit('logout');session_destroy();header('Location: ./');exit;}

// One-time first install.
if(!forge_setup_ready()){
  $setup_error=''; forge_setup_key();
  if($_SERVER['REQUEST_METHOD']==='POST'&&isset($_POST['setup_forge'])){
    csrf_check();$key=trim((string)($_POST['setup_key']??''));$user=trim((string)($_POST['admin_user']??''));$pass=(string)($_POST['admin_password']??'');$confirm=(string)($_POST['admin_password_confirm']??'');
    if(forge_setup_key()===''||!hash_equals(forge_setup_key(),$key))$setup_error='The setup key is not correct.';
    elseif($user==='')$setup_error='Enter an administrator username or email.';
    elseif(strlen($pass)<12)$setup_error='Use a password of at least 12 characters.';
    elseif($pass!==$confirm)$setup_error='The passwords do not match.';
    elseif(!forge_write_local_config($user,$pass))$setup_error='Forge could not write its private configuration. Set forge-app/storage to 775 and try again.';
    else{header('Location: ./');exit;}
  }
  ?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Set up Dream Kinetic: Forge</title><link rel="stylesheet" href="assets/app.css"></head><body class="login-body"><div class="login-card"><div class="eyebrow">DREAM KINETIC</div><div class="forge-mark">FORGE</div><p class="muted">First-time setup · F0.4.1 Railway Foundation</p><?php if($setup_error):?><div class="error"><?=h($setup_error)?></div><?php endif;?><div class="warning">Open <code>forge-app/storage/install-key.txt</code> in cPanel and paste the one-time key below.</div><form method="post"><input type="hidden" name="csrf" value="<?=h(csrf_token())?>"><label>One-time setup key<input name="setup_key" required autocomplete="off"></label><label>Administrator username / email<input name="admin_user" required autocomplete="username"></label><label>Password<input type="password" name="admin_password" minlength="12" required autocomplete="new-password"></label><label>Confirm password<input type="password" name="admin_password_confirm" minlength="12" required autocomplete="new-password"></label><button class="primary" name="setup_forge" value="1">Complete Forge setup</button></form></div></body></html><?php exit;
}

// Contractor invite acceptance lives before login.
if(isset($_GET['invite'])){
  $invite_error='';$token=trim((string)$_GET['invite']);
  if($_SERVER['REQUEST_METHOD']==='POST'&&isset($_POST['accept_invite'])){csrf_check();$pass=(string)($_POST['password']??'');$confirm=(string)($_POST['confirm']??'');if(strlen($pass)<12)$invite_error='Use at least 12 characters.';elseif($pass!==$confirm)$invite_error='Passwords do not match.';elseif(forge_accept_invite($token,$pass)){?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Forge access ready</title><link rel="stylesheet" href="assets/app.css"></head><body class="login-body"><div class="login-card"><div class="eyebrow">DREAM KINETIC</div><div class="forge-mark">FORGE</div><h2>Access activated.</h2><p class="muted">Your contractor account is ready. Sign in with your email and new password.</p><a class="primary-link" href="./">Go to Forge login</a></div></body></html><?php exit;}else$invite_error='This invitation is invalid or expired.';}
  ?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Accept Forge invitation</title><link rel="stylesheet" href="assets/app.css"></head><body class="login-body"><div class="login-card"><div class="eyebrow">DREAM KINETIC</div><div class="forge-mark">FORGE</div><p class="muted">Contractor invitation</p><?php if($invite_error):?><div class="error"><?=h($invite_error)?></div><?php endif;?><form method="post"><input type="hidden" name="csrf" value="<?=h(csrf_token())?>"><label>Create password<input type="password" name="password" minlength="12" required></label><label>Confirm password<input type="password" name="confirm" minlength="12" required></label><button class="primary" name="accept_invite" value="1">Activate account</button></form></div></body></html><?php exit;
}

$error='';
if($_SERVER['REQUEST_METHOD']==='POST'&&isset($_POST['login'])){
  csrf_check();$identity=forge_login_identity(trim((string)($_POST['user']??'')),(string)($_POST['pass']??''));
  if($identity){session_regenerate_id(true);$_SESSION['forge_identity']=$identity;audit('login');header('Location: ./');exit;}$error='Unable to sign in. Check your Forge credentials.';
}
if(!logged_in()){
  ?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Dream Kinetic: Forge</title><link rel="stylesheet" href="assets/app.css"></head><body class="login-body"><div class="login-card"><div class="eyebrow">DREAM KINETIC</div><div class="forge-mark">FORGE</div><p class="muted">Conversational, visual and code-capable application development platform</p><?php if($error):?><div class="error"><?=h($error)?></div><?php endif;?><form method="post"><input type="hidden" name="csrf" value="<?=h(csrf_token())?>"><label>Email / username<input name="user" required autocomplete="username"></label><label>Password<input type="password" name="pass" required autocomplete="current-password"></label><button class="primary" name="login" value="1">Enter Forge</button></form></div></body></html><?php exit;
}

$flash='';$flashType='success';$inviteUrl='';
$full=forge_state();$d=scoped_state($full);$apps=all_apps($d);

if($_SERVER['REQUEST_METHOD']==='POST'&&!isset($_POST['login'])){
  csrf_check();
  try{
    if(isset($_POST['new_client'])&&is_agency_admin()){if(forge_create_client(trim((string)$_POST['client_name'])))$flash='Client created.';}
    elseif(isset($_POST['new_contractor'])&&is_agency_admin()){
      $token=forge_invite_contractor(trim((string)$_POST['name']),trim((string)$_POST['email']),trim((string)$_POST['application_id']),trim((string)($_POST['ai_plan']??'')));
      if($token){$inviteUrl=(isset($_SERVER['HTTPS'])&&$_SERVER['HTTPS']!=='off'?'https':'http').'://'.($_SERVER['HTTP_HOST']??'localhost').($_SERVER['SCRIPT_NAME']??'/index.php').'?invite='.$token;$flash='Contractor invitation created. Copy the invitation URL below.';} else{$flash='Unable to create contractor invitation.';$flashType='error';}
    }
    elseif(isset($_POST['new_brief'])){$appId=trim((string)$_POST['application_id']);if(!authorize_app($full,$appId))throw new RuntimeException('Not authorized for this application.');if(forge_create_brief($appId,trim((string)$_POST['title']),trim((string)$_POST['objective']),trim((string)($_POST['risk']??'R1'))))$flash='Build Brief created.';}
    elseif(isset($_POST['new_repo'])){$appId=trim((string)$_POST['application_id']);if(!authorize_app($full,$appId))throw new RuntimeException('Not authorized for this application.');if(forge_add_repository($appId,trim((string)$_POST['repository']),trim((string)($_POST['branch']??'main'))))$flash='Repository registered.';else{$flash='Repository must use owner/repository format.';$flashType='error';}}
    elseif(isset($_POST['verify_repo'])){$appId=(string)$_POST['application_id'];$repo=(string)$_POST['repository'];if(!authorize_app($full,$appId))throw new RuntimeException('Not authorized for this application.');$probe=github_repo_probe($repo);forge_mark_repo_status($appId,$repo,$probe);$flash=$probe['ok']?'GitHub repository verified.':($probe['note']??'Repository could not be verified.');$flashType=$probe['ok']?'success':'warning';}
    elseif(isset($_POST['configure_database'])&&is_agency_admin()){
      $url=trim((string)$_POST['database_url']);parse_database_url($url);forge_pdo($url);forge_write_infrastructure(['storage_driver'=>'pgsql','database_url'=>$url]);$ran=run_postgres_migrations($url);seed_postgres_from_json($url);$flash='PostgreSQL connected, migrated and seeded. '.count($ran).' migration files applied.';
    }
    elseif(isset($_POST['use_json_recovery'])&&is_agency_admin()){forge_write_infrastructure(['storage_driver'=>'json-dev']);$flash='Forge switched to JSON recovery mode.';$flashType='warning';}
  }catch(Throwable $e){$flash=$e->getMessage();$flashType='error';}
  $full=forge_state();$d=scoped_state($full);$apps=all_apps($d);
}

if(isset($_GET['agent_pack'])){
  $appId=(string)$_GET['agent_pack'];if(!authorize_app($full,$appId)){http_response_code(403);exit('Not authorized');}$p=make_agent_pack($full,$appId);if(!$p){http_response_code(404);exit('Unknown application');}$fmt=$_GET['format']??'agents';audit('agent_pack.exported',['application_id'=>$appId,'format'=>$fmt]);if($fmt==='json'){header('Content-Type: application/json');header('Content-Disposition: attachment; filename="forge.context.json"');echo json_encode($p['context'],JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES);}elseif($fmt==='claude'){header('Content-Type:text/markdown');header('Content-Disposition: attachment; filename="CLAUDE.md"');echo $p['claude'];}else{header('Content-Type:text/markdown');header('Content-Disposition: attachment; filename="AGENTS.md"');echo $p['agents'];}exit;
}

$view=$_GET['view']??'overview';$mode=$_GET['mode']??'builder';$identity=current_user();$storage=storage_status();$https=https_status();$checks=certification_checks($full);
function navx($id,$label,$icon='•'){global $view;echo '<a class="nav '.($view===$id?'active':'').'" href="?view='.$id.'"><span>'.$icon.'</span>'.h($label).'</a>';}
?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Dream Kinetic: Forge</title><link rel="stylesheet" href="assets/app.css"></head><body>
<header><div><span class="brand-small">DREAM KINETIC</span><strong>FORGE</strong></div><div class="project-switch">Agency / Dream Kinetic <span>⌄</span></div><div class="mode-switch"><a class="<?=$mode==='builder'?'selected':''?>" href="?view=studio&mode=builder">Builder</a><a class="<?=$mode==='developer'?'selected':''?>" href="?view=studio&mode=developer">Developer</a></div><div class="header-actions"><span class="env-badge">F0.4 Production Foundation</span><span class="mini-user"><?=h($identity['name']??'User')?></span><a class="avatar" href="?logout=1">↗</a></div></header>
<div class="shell"><aside><div class="section-label">AGENCY</div><?php navx('overview','Overview','⌂');if(is_agency_admin())navx('clients','Clients','◫');navx('applications','Applications','▦');if(is_agency_admin())navx('contractors','Contractors','♙');?><div class="section-label">BUILD</div><?php navx('studio','Forge Studio','✦');navx('builds','Build Briefs','⌁');navx('dossiers','Design Dossiers','▤');navx('agent-packs','AI Context Packs','◎');?><div class="section-label">ENGINEER</div><?php navx('repositories','Repositories','⑂');navx('environments','Environments','◈');navx('architecture','Architecture','◇');navx('certification','Certification','✓');if(is_agency_admin())navx('infrastructure','Infrastructure','⚙');?><div class="spacer"></div><div class="version">Forge <?=h(forge_config()['version'])?><br><span><?=h($storage['driver'])?></span></div></aside><main>
<?php if($flash):?><div class="flash <?=$flashType?>"><b><?=h($flash)?></b><?php if($inviteUrl):?><div class="invite-copy"><input value="<?=h($inviteUrl)?>" readonly id="inviteUrl"><button onclick="navigator.clipboard.writeText(document.getElementById('inviteUrl').value)">Copy invite URL</button></div><?php endif;?></div><?php endif;?>

<?php if($view==='overview'):?>
<div class="page-head"><div><p class="kicker">DREAM KINETIC</p><h1>Agency application development platform.</h1><p>Research, design, build, preview, govern and release isolated client applications.</p></div><a class="primary-link" href="?view=studio">Open Forge Studio →</a></div>
<div class="stat-grid"><article><b><?=count($d['clients'])?></b><span>Visible clients</span></article><article><b><?=count($apps)?></b><span>Applications</span></article><article><b><?=count(array_filter($full['users']??[],fn($u)=>($u['type']??'')==='contractor'))?></b><span>Contractors</span></article><article><b><?=count($d['briefs']??[])?></b><span>Build briefs</span></article></div>
<div class="foundation-grid"><section class="card"><div class="panel-title">Production foundation <span class="good">● F0.4</span></div><div class="checklist"><div><span>✓</span><b>Agency → Client → Workspace → Application → Environment</b><small>Canonical hierarchy enforced</small></div><div><span>✓</span><b>Contractor invitation + app-scoped access</b><small>Independent contractor accounts can be activated without sharing AI credentials</small></div><div><span>✓</span><b>Prompt-to-Build Studio lifecycle</b><small>Explore → Research → Design → Build → Preview remains explicit</small></div><div><span>✓</span><b>GitHub repository registry</b><small>Public repositories can be verified from Forge</small></div><div><span><?=$storage['driver']==='PostgreSQL'&&$storage['ready']?'✓':'○'?></span><b>PostgreSQL production authority</b><small><?=h($storage['note'])?></small></div></div></section><section class="card"><div class="panel-title">Live readiness</div><div class="storage-box <?=$https['ready']?'ready':'pending'?>"><strong><?=h($https['label'])?></strong><p>HTTPS is required before production use.</p></div><div class="storage-box <?=($storage['driver']==='PostgreSQL'&&$storage['ready'])?'ready':'pending'?>"><strong><?=h($storage['driver'])?></strong><p><?=h($storage['note'])?></p></div><?php if(is_agency_admin()):?><a class="ghost-link infra-link" href="?view=infrastructure">Configure infrastructure →</a><?php endif;?></section></div>

<?php elseif($view==='clients'&&is_agency_admin()):?>
<div class="page-head"><div><p class="kicker">AGENCY</p><h1>Clients</h1><p>Every client is isolated beneath Dream Kinetic.</p></div><button class="primary" onclick="document.getElementById('clientDialog').showModal()">+ New client</button></div><div class="client-grid"><?php foreach($d['clients'] as $c):?><article class="client-card"><div class="client-icon"><?=h(strtoupper(substr($c['name'],0,2)))?></div><div><h3><?=h($c['name'])?></h3><p><?=count($c['workspaces']??[])?> workspace(s)</p><div class="tags"><?php foreach($c['workspaces']??[] as $w)foreach($w['applications']??[] as $a):?><span><?=h($a['name'])?></span><?php endforeach;?></div></div><span class="status"><?=h($c['status'])?></span></article><?php endforeach;?></div><dialog id="clientDialog"><form method="post"><input type="hidden" name="csrf" value="<?=h(csrf_token())?>"><h3>Create client</h3><label>Client name<input name="client_name" required></label><div class="dialog-actions"><button type="button" class="ghost" onclick="this.closest('dialog').close()">Cancel</button><button class="primary" name="new_client" value="1">Create</button></div></form></dialog>

<?php elseif($view==='applications'):?>
<div class="page-head"><div><p class="kicker">PORTFOLIO</p><h1>Applications</h1><p>One governed record for every app Forge can build or maintain.</p></div></div><section class="card table-card"><table><thead><tr><th>APPLICATION</th><th>CLIENT / WORKSPACE</th><th>ARCHITECTURE</th><th>RUNTIME</th><th>REPOSITORY</th><th>ENVIRONMENTS</th></tr></thead><tbody><?php foreach($apps as $a):?><tr><td><b><?=h($a['name'])?></b><small><?=h($a['id'])?></small></td><td><?=h($a['_client'])?><small><?=h($a['_workspace'])?></small></td><td><?=h($a['architecture'])?></td><td><?=h($a['runtime'])?></td><td><code><?=h($a['repo']?:'Not connected')?></code></td><td><?=h(implode(' · ',$a['environments']??[]))?></td></tr><?php endforeach;?></tbody></table></section>

<?php elseif($view==='contractors'&&is_agency_admin()):?>
<div class="page-head"><div><p class="kicker">ACCESS</p><h1>Contractors</h1><p>Contractors use their own approved AI subscriptions and receive only assigned Forge application access.</p></div><button class="primary" onclick="document.getElementById('contractorDialog').showModal()">+ Invite contractor</button></div><section class="card table-card"><table><thead><tr><th>CONTRACTOR</th><th>AI PLAN</th><th>ASSIGNMENT</th><th>STATUS</th></tr></thead><tbody><?php foreach($full['users']??[] as $u): if(($u['type']??'')!=='contractor') continue; $assign=array_values(array_filter($full['assignments']??[],fn($a)=>$a['user_id']===$u['id']));?><tr><td><b><?=h($u['name'])?></b><small><?=h($u['email'])?></small></td><td><?=h($u['ai']??'')?></td><td><?php foreach($assign as $x):?><span class="tagline"><?=h($x['client_id'].' / '.$x['application_id'].' · '.$x['access'])?></span><?php endforeach;?></td><td><span class="status"><?=h($u['status'])?></span></td></tr><?php endforeach;?></tbody></table></section><dialog id="contractorDialog"><form method="post"><input type="hidden" name="csrf" value="<?=h(csrf_token())?>"><h3>Invite contractor</h3><label>Name<input name="name" required></label><label>Email<input type="email" name="email" required></label><label>Application<select name="application_id"><?php foreach(all_apps($full) as $a):?><option value="<?=h($a['id'])?>"><?=h($a['_client'].' — '.$a['name'])?></option><?php endforeach;?></select></label><label>AI subscription<input name="ai_plan" placeholder="ChatGPT Plus / Claude Pro"></label><div class="dialog-actions"><button type="button" class="ghost" onclick="this.closest('dialog').close()">Cancel</button><button class="primary" name="new_contractor" value="1">Create invitation</button></div></form></dialog>

<?php elseif($view==='studio'):?>
<div class="studio-grid <?=$mode==='developer'?'developer':''?>"><section class="conversation"><div class="panel-title"><span><i class="status-dot"></i>Build with Avery</span><small>Roll Call context active</small></div><div class="stage-strip" id="stageStrip"><button class="active" data-stage="explore">Explore</button><button data-stage="research">Research</button><button data-stage="design">Design</button><button data-stage="build">Build</button></div><div class="conversation-scroll" id="conversation"><div class="message ai"><strong>Avery</strong>Tell me what you want to create or change. We can discuss it first, research it, design a direction, or build against a preview branch.</div><div class="message user">I want event cards to feel more visual and premium.</div><div class="message ai"><strong>Avery</strong>I can explore the interaction, research references, propose design directions, or make the change in the preview branch. Nothing is published automatically.</div><div class="chips"><button data-prompt-chip="Research event discovery card patterns">Research</button><button data-prompt-chip="Propose three design directions without changing code">Ideate</button><button data-prompt-chip="Build the approved direction in preview">Build in preview</button></div></div><div class="promptbox"><textarea id="prompt" placeholder="Ask Avery to connect, ideate, research, design or build..."></textarea><div><select id="aiProvider"><option>Avery</option><option>ChatGPT / Codex</option><option>Claude</option><option>Avery + external AI</option></select><button class="primary" id="sendPrompt">Send</button></div></div></section><section class="canvas"><div class="canvas-toolbar"><div><b><?=$mode==='developer'?'Development workspace':'Live application preview'?></b><span class="pill">PREVIEW ONLY</span></div><div class="devices"><button class="selected" data-device="desktop">Desktop</button><button data-device="tablet">Tablet</button><button data-device="mobile">Mobile</button></div></div><?php if($mode==='developer'):?><div class="dev-workspace"><div class="code-tabs"><span>Generated plan</span> &nbsp; EventCard.tsx &nbsp; policy.json &nbsp; tests</div><pre id="codePreview">// Forge Developer Mode\n// Builder and Developer Mode operate on the same application.\n\nexport const EventCard = ({ event }) =&gt; {\n  return &lt;article data-forge-component=\"EventCard\"&gt;\n    {/* governed component implementation */}\n  &lt;/article&gt;;\n};\n\n// Current AI stage: Explore\n// Source mutation: blocked until Build stage\n// Tenant context: required\n// Design dossier: required\n</pre><div class="terminal"><span>✓</span> Preview workspace isolated · architecture context loaded · no production mutation</div></div><?php else:?><div class="preview-wrap"><div class="preview desktop" id="preview"><div class="app-preview"><div class="app-top"><b>EVENTS</b><span>Las Vegas</span><span class="mini-avatar">TH</span></div><div class="app-content"><p class="kicker">UPCOMING</p><h1>Experiences worth showing up for.</h1><div class="metric-row"><div><b>18</b><span>Upcoming</span></div><div><b>7</b><span>Venues</span></div><div><b>84%</b><span>On track</span></div></div><div class="preview-card" id="targetCard" data-forge-component="EventCard"><div><small>FRIDAY · 9:00 PM</small><h3>The Get Down</h3><p id="previewDescription">A premium event card rendered from the current preview state.</p></div><button>Open →</button></div><div class="cards"><div><b>NOVA</b><br>Sat · 10 PM</div><div><b>House Party</b><br>Sun · 8 PM</div><div><b>After Hours</b><br>Thu · 9 PM</div></div></div></div></div></div><?php endif;?></section><section class="inspector"><div class="panel-title">Build Inspector</div><div class="inspect-tabs"><b>Context</b><span>Changes</span></div><label>Application<select><option>Roll Call Events</option><option>BRDCST</option></select></label><label>Environment<select><option>Development / Preview</option></select></label><label>AI stage<input id="inspectorStage" value="Explore" readonly></label><div class="toggle-row"><span>Design Dossier</span><b>Required</b></div><div class="toggle-row"><span>Tenant isolation</span><b>On</b></div><div class="toggle-row"><span>Auto publish</span><b>Off</b></div><hr><div class="panel-title small">Build timeline</div><div class="timeline"><div class="current"><b>Current preview</b><span>No source mutation</span></div><div><b>Certified state</b><span>Last known good</span></div></div></section></div>

<?php elseif($view==='builds'):?>
<div class="page-head"><div><p class="kicker">INTENT</p><h1>Build Briefs</h1><p>Approved intent before AI or contractors execute material changes.</p></div><button class="primary" onclick="document.getElementById('briefDialog').showModal()">+ New Build Brief</button></div><div class="brief-grid"><?php foreach($d['briefs']??[] as $b):?><article class="brief-card"><small><?=h($b['id'])?> · <?=h($b['stage'])?></small><h3><?=h($b['title'])?></h3><p><?=h($b['objective'])?></p><span class="risk"><?=h($b['risk'])?></span></article><?php endforeach;?></div><dialog id="briefDialog"><form method="post"><input type="hidden" name="csrf" value="<?=h(csrf_token())?>"><h3>Create Build Brief</h3><label>Application<select name="application_id"><?php foreach($apps as $a):?><option value="<?=h($a['id'])?>"><?=h($a['_client'].' — '.$a['name'])?></option><?php endforeach;?></select></label><label>Title<input name="title" required></label><label>Objective<textarea name="objective" required></textarea></label><label>Risk<select name="risk"><option>R1</option><option>R2</option><option>R3</option><option>R4</option></select></label><div class="dialog-actions"><button type="button" class="ghost" onclick="this.closest('dialog').close()">Cancel</button><button class="primary" name="new_brief" value="1">Create</button></div></form></dialog>

<?php elseif($view==='dossiers'):?>
<div class="page-head"><div><p class="kicker">DESIGN AUTHORITY</p><h1>Design Dossiers</h1><p>Current visual and interaction authority exposed to designers, contractors and AI.</p></div></div><section class="card table-card"><table><thead><tr><th>APPLICATION</th><th>DOSSIER</th><th>VERSION</th><th>STATUS</th></tr></thead><tbody><?php foreach($d['dossiers']??[] as $x):$a=find_app($d,$x['application_id']);?><tr><td><?=h($a['name']??$x['application_id'])?></td><td><b><?=h($x['title'])?></b></td><td><?=h($x['version'])?></td><td><span class="status"><?=h($x['status'])?></span></td></tr><?php endforeach;?></tbody></table></section>

<?php elseif($view==='agent-packs'):?>
<div class="page-head"><div><p class="kicker">AI CONTEXT</p><h1>AI Context Packs</h1><p>The same governed project context for Avery, ChatGPT/Codex and Claude.</p></div></div><div class="agent-grid"><?php foreach($apps as $a):?><article class="agent-card"><div><small><?=h($a['_client'])?></small><h3><?=h($a['name'])?></h3><p><?=h($a['architecture'])?> · <?=h($a['runtime'])?></p></div><div class="download-row"><a href="?agent_pack=<?=urlencode($a['id'])?>">AGENTS.md</a><a href="?agent_pack=<?=urlencode($a['id'])?>&format=claude">CLAUDE.md</a><a href="?agent_pack=<?=urlencode($a['id'])?>&format=json">Context JSON</a></div></article><?php endforeach;?></div>

<?php elseif($view==='repositories'):?>
<div class="page-head"><div><p class="kicker">SOURCE</p><h1>Repositories</h1><p>Map each application to its source repository and verify that Forge can resolve it.</p></div><button class="primary" onclick="document.getElementById('repoDialog').showModal()">+ Connect repository</button></div><section class="card table-card"><table><thead><tr><th>APPLICATION</th><th>GITHUB</th><th>BRANCH</th><th>STATUS</th><th></th></tr></thead><tbody><?php foreach($d['repositories']??[] as $r):$a=find_app($d,$r['application_id']);?><tr><td><?=h($a['name']??$r['application_id'])?></td><td><code><?=h($r['repository'])?></code></td><td><?=h($r['default_branch']??'main')?></td><td><span class="status <?=strtolower($r['status'])==='verified'?'':'muted-status'?>"><?=h($r['status'])?></span></td><td><form method="post"><input type="hidden" name="csrf" value="<?=h(csrf_token())?>"><input type="hidden" name="application_id" value="<?=h($r['application_id'])?>"><input type="hidden" name="repository" value="<?=h($r['repository'])?>"><button class="ghost" name="verify_repo" value="1">Verify</button></form></td></tr><?php endforeach;?></tbody></table></section><dialog id="repoDialog"><form method="post"><input type="hidden" name="csrf" value="<?=h(csrf_token())?>"><h3>Connect GitHub repository</h3><label>Application<select name="application_id"><?php foreach($apps as $a):?><option value="<?=h($a['id'])?>"><?=h($a['_client'].' — '.$a['name'])?></option><?php endforeach;?></select></label><label>Repository<input name="repository" placeholder="owner/repository" required></label><label>Default branch<input name="branch" value="main"></label><div class="dialog-actions"><button type="button" class="ghost" onclick="this.closest('dialog').close()">Cancel</button><button class="primary" name="new_repo" value="1">Connect</button></div></form></dialog>

<?php elseif($view==='environments'):?>
<div class="page-head"><div><p class="kicker">RUNTIME</p><h1>Environments</h1><p>Development, preview, staging and production remain distinct release targets.</p></div></div><div class="env-grid"><?php foreach($apps as $a):?><article class="env-card"><small><?=h($a['_client'])?></small><h3><?=h($a['name'])?></h3><?php foreach($a['_environment_records']??array_map(fn($n)=>['name'=>$n,'status'=>'Ready'],$a['environments']??[]) as $e):?><div><span>●</span><span><?=h($e['name'])?></span><b><?=h(ucfirst($e['status']??'Ready'))?></b></div><?php endforeach;?></article><?php endforeach;?></div>

<?php elseif($view==='architecture'):?>
<div class="page-head"><div><p class="kicker">GOVERNANCE</p><h1>Architecture</h1><p>Forge builds against declared platform/runtime contracts rather than hidden implementation coupling.</p></div></div><div class="foundation-grid"><section class="card architecture-stack"><div>Dream Kinetic: Forge<span>Agency development platform</span></div><div>Forge Runtime Standard 1.0<span>Framework-first runtime beneath Forge contracts</span></div><div>Client Architecture Profiles<span>Roll Call Platform v3 or client-specific profile</span></div><div>Data Authority<span>PostgreSQL production target · JSON recovery adapter only</span></div></section><section class="card"><div class="panel-title">Non-negotiable runtime rules</div><div class="checklist"><div><span>✓</span><b>Client isolation is explicit</b><small>No ambient cross-client access.</small></div><div><span>✓</span><b>Builder and Developer Mode share one application</b><small>No parallel low-code/code products.</small></div><div><span>✓</span><b>AI discussion is not execution</b><small>Explore/Research/Design cannot mutate source.</small></div><div><span>✓</span><b>Preview precedes release</b><small>Publishing remains a separate governed lifecycle.</small></div></div></section></div>

<?php elseif($view==='certification'):?>
<div class="page-head"><div><p class="kicker">RELEASE GATES</p><h1>Certification</h1><p>F0.4 reports what is genuinely ready and what still blocks production certification.</p></div></div><section class="card cert-card"><?php foreach($checks as $c):?><div class="cert-row <?=$c['ok']?'':'pending'?>"><span><?=$c['ok']?'✓':'○'?></span><div><b><?=h($c['name'])?></b><small><?=h($c['detail'])?></small></div></div><?php endforeach;?></section>

<?php elseif($view==='infrastructure'&&is_agency_admin()):?>
<div class="page-head"><div><p class="kicker">PRODUCTION FOUNDATION</p><h1>Infrastructure</h1><p>Connect secure transport and canonical persistence before F1.0 starts generating real applications.</p></div></div><div class="foundation-grid"><section class="card"><div class="panel-title">HTTPS</div><div class="storage-box <?=$https['ready']?'ready':'pending'?>"><strong><?=h($https['label'])?></strong><p><?= $https['ready']?'Secure transport is active.':'Enable cPanel AutoSSL for forge.dreamkinetic.com and redirect HTTP to HTTPS.'?></p></div></section><section class="card"><div class="panel-title">PostgreSQL</div><div class="storage-box <?=($storage['driver']==='PostgreSQL'&&$storage['ready'])?'ready':'pending'?>"><strong><?=h($storage['driver'])?></strong><p><?=h($storage['note'])?></p><small>pdo_pgsql: <?=extension_loaded('pdo_pgsql')?'available':'not enabled'?></small></div><form method="post" class="infra-form"><input type="hidden" name="csrf" value="<?=h(csrf_token())?>"><label>PostgreSQL connection URL<input type="password" name="database_url" placeholder="postgresql://user:password@host:5432/database?sslmode=require" required></label><p class="muted">Railway, managed PostgreSQL, or a cPanel PostgreSQL server can be used. The URL is stored in Forge's blocked private storage directory with restrictive file permissions.</p><button class="primary" name="configure_database" value="1">Connect, migrate & seed</button></form><?php if($storage['driver']==='PostgreSQL'):?><form method="post" class="recovery-form"><input type="hidden" name="csrf" value="<?=h(csrf_token())?>"><button class="ghost" name="use_json_recovery" value="1">Use JSON recovery adapter</button></form><?php endif;?></section></div>

<?php else:?><div class="empty-state"><div>◇</div><h2><?=h(ucwords($view))?></h2><p>This surface is registered for a future Forge release.</p></div><?php endif;?>
</main></div><script src="assets/app.js"></script></body></html>
