{
  "name": "@roll-call/platform-gateway",
  "version": "3.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test.mjs",
    "check": "node --check server.mjs && node --check browser-session.mjs && node --check test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
